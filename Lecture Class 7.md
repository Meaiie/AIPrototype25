# Web Service
> มีหน้าที่ประมวลผลระหว่างโปรแกรม
> รับมา แล้ว ส่งเครดิตไปให้ปลายทาง

# Web Service for Sending Messages
> เป็น Web Service ที่สามารถส่งข้อความระหว่างผู้ใช้ได้ โดยประกอบไปด้วย 2 ส่วนหลัก:

## 1. **สคริปต์ฝั่งผู้ใช้**
> ช่วยให้ผู้ใช้ป้อนข้อความและเลือกผู้รับเพื่อส่งข้อความ
สคริปต์ฝั่งผู้ใช้จะติดต่อกับ API ฝั่งเซิร์ฟเวอร์เพื่อส่งข้อความ โดยมีขั้นตอนดังนี้:
- ผู้ใช้จะป้อนข้อความที่ต้องการส่ง
- ผู้ใช้สามารถเลือกผู้รับได้จาก IP Address
- ส่งข้อความที่เลือกไปยังเซิร์ฟเวอร์ผ่านคำขอ HTTP POST

สคริปต์จะส่งข้อมูลต่อไปนี้ไปยังเซิร์ฟเวอร์:
- `msg`: ข้อความที่ผู้ใช้ป้อน
- `ผู้รับ`: ชื่อของผู้รับข้อความ
- `ip`: ที่อยู่ IP ของผู้รับ
- `ผู้ส่ง`: ชื่อของผู้ส่งข้อความ

```python
import requests
import json

url = 'http://localhost:5001/request_POSTGET'
myobj = {'name':'Siriyaporn',
         'age':21
         }

x = requests.post(url, data = json.dumps(myobj))
output = json.loads(x.text)
print(output['y'])
```

## 2. **API ฝั่งเซิร์ฟเวอร์** [`firstflask.py`](https://github.com/Meaiie/AIPrototype25/blob/main/myfirstflask.py): 
> รับข้อความจากผู้ใช้ บันทึกรายละเอียด และส่งคำตอบกลับไปยืนยันการรับข้อความ

```python
@app.route('/receive_get',methods=['GET']) # ไม่ค่อยปลอดภัยเพราะส่งผ่าน url เลย
def web_service_API_GET():

    msg = request.args.get('msg')
    name = request.args.get('name')

    print(f'the input message from GET is {msg} from {name}.')

    return f'{msg} from {name} received !'
```

### เขียนฟังก์ชันให้รับได้ทั้ง GET และ POST
#### Test Get
> ส่งมากกว่าหนึ่งตัวแปรโดยใช้ &<br>
```
http://localhost:5001/function_name?msg=what&name=Meaiie
```
<img width="748" height="169" alt="image" src="https://github.com/user-attachments/assets/f423262d-6982-415d-b528-7aada28300fa" /><br>

#### Test POST<br>
> **[`test_post.py`](https://github.com/Meaiie/AIPrototype25/blob/main/test_post.py)** <br>
> ถ้าเราเปลี่ยนชื่อฟังก์ชันต้องไปแก้ไฟล์ test ให้ชื่อฟังก์ชันตรงกัน
> <img width="741" height="355" alt="image" src="https://github.com/user-attachments/assets/8e2d5e4f-905f-462b-a3fe-9276d36cbe98" /><br>

## ลองให้ ai generate code ง่าย ๆ ให้
🌸 Iris Species Classification Web App

โปรเจกต์ Web Application สำหรับจำแนกสายพันธุ์ดอก Iris (Iris Flower Classification) โดยใช้ **Machine Learning (Random Forest)** เชื่อมต่อกับ **Flask Framework** เพื่อแสดงผลผ่านหน้าเว็บที่สวยงามและทันสมัย

![Python](https://img.shields.io/badge/Python-3.8%2B-blue)
![Flask](https://img.shields.io/badge/Flask-2.0%2B-green)
![Scikit-Learn](https://img.shields.io/badge/Sklearn-RandomForest-orange)

### ✨ ฟีเจอร์หลัก (Features)
- **AI Prediction:** ทำนายสายพันธุ์ดอกไม้ (Setosa, Versicolor, Virginica) แม่นยำด้วย Random Forest Classifier
- **Modern UI:** หน้าเว็บตกแต่งด้วย CSS Glassmorphism และ Responsive Design
- **Interactive:** มีระบบ Validation ป้องกันการกรอกค่าติดลบ และแสดงผลรูปภาพดอกไม้จริงตามผลลัพธ์
- **API Support:** รองรับการส่ง Request ทั้งแบบ GET และ POST สำหรับการทดสอบ API

### 📂 โครงสร้างโปรเจกต์ (Project Structure)
```text
/AIPrototype25
│
├── train.py            # Script สำหรับเทรนโมเดล (รันครั้งแรกครั้งเดียว)
├── web_app.py          # Main application file (Flask Server)
├── iris_model.pkl      # Pre-trained Model
│
├── static/             # Static files (CSS, JS, Images)
│   ├── style.css       # การตกแต่งหน้าเว็บ (Glassmorphism theme)
│   ├── script.js       # Client-side validation & loading effect
│   └── images/         # รูปภาพดอกไม้ (setosa.jpg, etc.)
│
├── templates/          # HTML Templates
    └── index.html      # หน้า User Interface หลัก
```
### ขั้นตอนที่ 1 : สร้าง script สำหรับเทรนโมเดล
**Code :** **[`Train_model.py`](https://github.com/Meaiie/AIPrototype25/blob/main/train.py)**
> เมื่อเราเทรนโมเดลสำเร็จ<br>
> <img width="741" height="92" alt="image" src="https://github.com/user-attachments/assets/d83a91b3-e1f2-4a02-910d-147b09f45892" /><br>

### ขั้นตอนที่ 2 : สร้าง Flask server
**Code :** **[`Web_app.py`](https://github.com/Meaiie/AIPrototype25/blob/main/web_app.py)**

### ขั้นตอนที่ 3 : สร้าง Web Frontend
> เขียน html ได้ 2 แบบ
#### 1.📄 HTML Inline
> เขียน HTML, CSS และ JavaScript รวมอยู่ในไฟล์เดียว<br>
> แทรกอยู่ใน python ได้<br>
> ทุกอย่างอยู่ในไฟล์เดียว ไม่ต้องจัดการโครงสร้างไฟล์<br>
#### 2.📄 HTML แบบแยกไฟล์
> แยกไฟล์ตามหน้าที่ของโค้ด<br>
> โครงสร้างทั่วไป : <br>
```
index.html — โครงสร้างหน้าเว็บ

style.css — การตกแต่ง

script.js — การทำงานของระบบ
```
#### ตัวอย่างหน้าเว็บ
<img width="436" height="277" alt="image" src="https://github.com/user-attachments/assets/3ddbfe25-3894-4618-9ac2-2a9350ffb838" />

### ทำให้หน้าเว็บของเราดูดีขึ้นด้วย CSS และ JavaScript
**CSS Code :** **[`stlye.css`](https://github.com/Meaiie/AIPrototype25/blob/main/static/style.css)**<br>
**javascript Code :** **[`script.js`](https://github.com/Meaiie/AIPrototype25/blob/main/static/script.js)**<br>
#### **🖼️ ตัวอย่างหน้าเว็บ**
<img width="1054" height="873" alt="image" src="https://github.com/user-attachments/assets/a8645f42-45d7-4a19-809e-0811e170655e" />

**🛠️ Tech Stack**
```
Frontend: HTML5, CSS3 (Custom Glassmorphism), JavaScript
Backend: Python (Flask)
Machine Learning: Scikit-Learn (Random Forest)
Data Processing: Pandas, NumPy
```
#### โชว์ภาพเมื่อ predict
<img width="1022" height="798" alt="image" src="https://github.com/user-attachments/assets/907d5108-56c9-4231-b833-e6dc215bf26a" />

